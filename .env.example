# 声画工坊 · 密钥配置模板
# 用法：复制本文件为 .env（即 `cp .env.example .env`），填入你任意一个厂商的 key 即可。
# 程序启动时会自动探测存在的 key（无需安装 python-dotenv）。改完 .env 必须重启服务。

# ===================== 大模型（视频评论 / 字幕翻译）=====================
# —— 豆包 / 火山方舟（与抖音同源，效果最佳，推荐）——
# ARK_API_KEY=你的豆包方舟key

# —— DeepSeek ——
# DEEPSEEK_API_KEY=你的DeepSeek key

# —— 通义千问（阿里）——
# QWEN_API_KEY=你的通义key

# —— OpenAI ——
# OPENAI_API_KEY=你的OpenAI key

# 可选：显式指定用哪个厂商（不填则 auto 自动探测存在的 key）
# LLM_PROVIDER=auto

# 可选：覆盖该厂商的默认模型名
# LLM_MODEL=doubao-seed-1.6-250615

# ===================== 配音引擎（AI 配音 / 视频本地化流水线）=====================
# 真实配音走「火山引擎·豆包语音合成大模型 2.0」（seed-tts-2.0）。
# 只需填下面一个 API Key 即可启用真实人声；留空则使用 Mock 占位音轨。
# 获取：火山引擎控制台 → API 访问密钥（新版控制台）→ 复制 API Key
VOLC_TTS_API_KEY=

# 可选：模型版本（默认 seed-tts-2.0）
# VOLC_TTS_RESOURCE_ID=seed-tts-2.0

# 可选：覆盖默认音色（留空用内置映射；2.0 音色名需含 _uranus_bigtts）
# VOLC_TTS_SPEAKER=zh_female_shuangkuaisisi_uranus_bigtts

# —— 旧版控制台（二选一，非必须）：AppID + Access Token ——
# VOLC_APPID=
# VOLC_TOKEN=
